import 'package:flutter/material.dart';

class ComunityHead extends StatelessWidget {
  const ComunityHead({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
